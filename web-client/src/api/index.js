import { getAction, keys } from "../actions";
import { request  } from '../utils'

const { LOADING_PRODUCT, LOADNG_PRODUCT_LIST } = keys;

export const loadProductData = (conditions, offset, token) => {};

export const addProduct = (values) => {};
